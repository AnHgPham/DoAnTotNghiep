# Claude Instructions

Use `AGENTS.md` as the canonical project instruction file.

If this file is loaded by a Claude-style runtime, first read `AGENTS.md`, then `docs/agent_orchestration_init.md`, then `docs/current_agent_state.md`.
